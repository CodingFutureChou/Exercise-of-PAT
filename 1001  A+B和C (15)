#include<cstdio>

int main()
{
	long long a,b,c;
	int time;
	scanf("%d",&time);
	for(int i=0;i<time;i++)
	{
		scanf("%lld%lld%lld",&a,&b,&c);
		if(a+b>c) printf("Case #%d: true\n",i+1);
		else printf("Case #%d: false\n",i+1);
	}
	return 0;
 } 
